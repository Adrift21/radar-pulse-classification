# Python 3.11. Install with:  pip install -r requirements.txt

# === Core scientific stack ===
numpy>=1.24.0
scipy>=1.11.0
matplotlib>=3.7.0

# === Deep learning ===
torch>=2.1.0
torchvision>=0.16.0
timm>=0.9.0          # ResNet-50 and ViT-Small backbones

# === Signal processing ===
# The Choi-Williams and Wigner-Ville distributions are implemented from first
# principles in preprocessing/time_frequency/. tftb is retained only as the
# reference used to validate that implementation (see docs/decisions.md,
# 2026-05-17 entry); it is not imported by the training or analysis pipeline.
tftb>=0.2.0

# === Image processing ===
opencv-python>=4.8.0   # bilinear resize in preprocessing/transforms/tf_to_image.py

# === Machine learning utilities ===
scikit-learn>=1.3.0    # stratified splitting, classification metrics

# === Experiment tracking & config ===
PyYAML>=6.0
tensorboard>=2.14.0

# === HDF5 (MATLAB v7.3 dataset) ===
h5py>=3.9.0

# === Code quality ===
black>=23.9.0
ruff>=0.1.0
pytest>=7.4.0
